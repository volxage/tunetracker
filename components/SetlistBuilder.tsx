import {SafeAreaView} from "react-native";
import {BgView} from "../Style";

export default function SetlistBuilder({}:{}){
  return(
    <BgView>
      <SafeAreaView></SafeAreaView>
    </BgView>
  )
}
